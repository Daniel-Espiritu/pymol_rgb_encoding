This package was created to allow PyMol structure amino acid residues to be
systematically colored according to a given set of
numerical values and an assigned color gradient.

See ./pyml_rgb_encoding/test/test.py for example usage. Run the resulting .pml
file in PyMol to visualize the colored structure.

Dependencies include:

Python 3+ (numpy)
PyMol 2+
